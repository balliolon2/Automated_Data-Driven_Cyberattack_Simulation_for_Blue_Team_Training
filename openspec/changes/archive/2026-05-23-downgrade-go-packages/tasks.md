## 1. Package Downgrades

- [x] 1.1 Run `go get github.com/gin-contrib/cors@v1.7.2` to downgrade the cors middleware
- [x] 1.2 Run `go mod tidy` to clean up and stabilize `go.mod` and `go.sum` for Go 1.24.0
